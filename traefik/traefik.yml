entryPoints:
  web:
    address: ":80"

  websecure:
    address: ":443"

certificatesResolvers:
  myresolver:
    acme:
      email: acme@atherantimes.com
      storage: acme.json
      httpChallenge:
        # used during the challenge
        entryPoint: web
